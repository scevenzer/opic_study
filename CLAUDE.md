# OPIc 학습 앱 — 프로젝트 참고 문서

## 앱 개요
OPIc(오픽) 시험 준비를 위한 개인용 학습 웹앱. 토픽별 예상 질문 → 모범 답변 스크립트 →
한글 발음 가이드를 순서대로 학습하고, 문제별 완료 여부를 체크하며 진행률을 관리한다.
서버 없이 브라우저에서 파일을 열기만 하면 동작하는 단일 사용자용 정적 앱.

## 기술 스택 & 제약
- **순수 HTML/CSS/JS**, 프레임워크·빌드 도구·패키지 매니저 없음. `<script>` 태그로 직접 로드.
- **저장소**: `localStorage`(`opic-progress` 키)에 완료한 질문 ID만 저장. 서버·DB·계정 개념 없음 → 기기/브라우저 간 진행률 동기화 안 됨.
- **TTS**: 브라우저 내장 `speechSynthesis` API로 영어 스크립트 전체 읽기 (`en-US`, rate 0.85). 외부 음성 API 아님 → 음질/발음이 브라우저·OS에 따라 다름.
- **Git 저장소 아님** (버전 관리 미사용, 변경 이력 없음).

## 파일 구조
| 파일 | 역할 |
|---|---|
| `index.html` | 전체 UI(CSS 인라인) + 전체 로직(JS 인라인). 화면 렌더링, 네비게이션, localStorage, TTS, 완료 체크 등 모든 기능이 이 한 파일에 있음 |
| `data.js` | `TOPICS` 배열 하나만 정의. 토픽·문제·스크립트·발음 데이터 전체 (약 1,800줄, 16개 토픽 / 총 42문제) |

## data.js 스키마
```js
const TOPICS = [
  {
    id: Number,            // 토픽 ID
    emoji: String,         // 토픽 카드에 표시할 이모지
    nameKo: String,        // 토픽 한글명 (예: "집 / 동네")
    questions: [
      {
        id: Number,               // 질문 ID (토픽 간 전역 고유, progress 키로 사용)
        question: String,         // 영어 원문 질문
        translation: String,      // 질문 한글 번역
        type: String,             // 질문 유형 (예: "장소묘사", "문제 해결"). "롤플레이" 포함 시 UI에서 별도 색상 태그
        structure: [String],      // 답변 구성 뼈대(한국어 키워드 나열), '문제' 탭에 표시
        script: [                 // 모범 답변 스크립트, '스크립트' 탭 + TTS 소스
          { en: String, ko: String }
        ],
        pronunciation: [          // 문장 단위 한글 발음 표기, '발음' 탭
          { en: String, ko: String }
        ],
        warnings: [                // 발음 주의 단어 목록
          { word: String, guide: String }
        ]
      }
      // ...
    ]
  }
  // ...
];
```
- `script` / `pronunciation`이 없으면 UI가 "아직 준비 중" placeholder를 자동으로 표시함(index.html:569-601).
- 새 토픽/문제를 추가할 땐 이 구조를 그대로 따라 `data.js`에 객체를 추가하면 됨 — `index.html` 수정 불필요.

## 화면 흐름 (index.html)
```
home (토픽 그리드, 전체 진행률)
 ├─ topic (선택한 토픽의 질문 리스트, 토픽별 진행률)
 │   └─ question (문제 / 스크립트 / 발음 3개 탭 + 완료 체크 버튼)
 └─ phrases (토픽 무관 공통 구문 7개 카테고리, index.html 내 PHRASES 상수)
```
- 뒤로가기는 `screenStack` 배열로 직접 관리 (브라우저 히스토리 API 미사용).
- 화면 전환 시 TTS 재생 중이면 자동 취소.

## 알려진 제약 / 개선 아이디어
- 데이터가 하드코딩이라 문제 추가·수정 시 `data.js`를 직접 편집해야 함 (관리 UI 없음).
- 진행률이 기기 로컬에만 저장되어 여러 기기에서 학습 시 진행 상황이 어긋남.
- 브라우저 TTS라 발음 품질이 제한적 (특히 한글 발음 표기와 실제 TTS 발음이 다를 수 있음).
- 검색/필터 기능 없음 (토픽 그리드만으로 탐색).
- 오답 노트, 반복 학습(스페이스드 리피티션), 녹음 후 자가 비교 같은 학습 강화 기능은 아직 없음.

## 작업 시 참고사항
- 기존 패턴 유지: 프레임워크·빌드 도구 도입하지 말고 순수 HTML/CSS/JS로 작업.
- UI·로직 변경은 `index.html`, 콘텐츠(문제/스크립트/발음) 추가는 `data.js`에서.
- 새 기능 추가 시에도 "파일 열면 바로 동작"하는 정적 앱 특성을 유지할 것 (서버 필요한 구조 지양).
